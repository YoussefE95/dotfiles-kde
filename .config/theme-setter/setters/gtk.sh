#!/bin/bash

gsettings set org.gnome.desktop.interface font-name "Hack Nerd Font"
gsettings set org.gnome.desktop.interface gtk-theme "${palette[0]}"
gsettings set org.gnome.desktop.interface icon-theme "${palette[1]}"
# gsettings set org.gnome.desktop.interface cursor-theme "${palette[2]}"
